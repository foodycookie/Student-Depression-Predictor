Type the below in the terminal to download all library:
pip install -r requirements.txt

Please run "main.py" before running "app.py"

Type the below in the terminal to run "app.py":
python -m streamlit run app.py